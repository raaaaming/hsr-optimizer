/**
 * 캐릭터 전용 광추.
 *
 * 캐릭터 slug => 광추 slug.
 *
 * Yatta에는 광추와 캐릭터를 잇는 필드가 없다. 승급 재료가 겹치긴 하지만
 * 그건 운명과 월드가 같아서지 전용이라서가 아니다(반디의 승급 재료
 * 114011은 같은 월드의 다른 캐릭터도 쓴다). 그래서 손으로 적는다.
 *
 * lightConePassives.js와 같은 성격의 파일이다. 데이터가 알려주지 않는 걸
 * 사람이 적고, npm run data:check가 오타를 잡는다.
 *
 * 여기 없는 캐릭터는 전용 광추가 목록 맨 위로 오지 않을 뿐, 나머지는
 * 5성 => 4성 => 3성 순으로 정상 동작한다.
 *
 * 추가할 때는 게임에서 실제로 확인하고 적어라. 짐작으로 적으면
 * 엉뚱한 광추가 "전용"으로 추천된다.
 */
export const SIGNATURE_LIGHT_CONES = {

    // 반디 — 「완전연소」/격파 특화. 궤멸 상태를 거는 격파 특수효과 광추다.
    firefly: "whereabouts-should-dreams-rest"

};
